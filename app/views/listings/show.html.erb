<p>
	Type:<%= @listing.type %> <br>
	City: <%= @listing.city %><br>
	Bedrooms: <%= @listing.num_bed %><br>
	Bathrooms: <%= @listing.num_bath %><br>
	Price: $ <%= @listing.price %><br>
	Description: <%= @listing.description %><br>
	Photos: <%= image_tag @listing.photo.url, height: '150', width: '130' %><br>

	<% @listing.photos.each do |p| %>
	  <%= image_tag p.photo.url, height: '250', width: '230' %>
	  <%= link_to "Delete this photo", :class => "btn btn-default" %>
	<% end %>

  <%= link_to "Edit Listing", edit_listing_path,  :class => "btn btn-default" %>
  <%= link_to "Remove listing", :listing, method: :delete, data: { confirm: "Are you sure you want to delete this listing?"},  :class => "btn btn-default" %>
  <%= link_to "Add Photos", new_listing_photo_path(@listing), :class => "btn btn-default"  %>
  <%= link_to "Back to the list", listings_path, :class => "btn btn-default" %>
</p>