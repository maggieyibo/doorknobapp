<!DOCTYPE html>
<html>
<head>
  <title>Doorknobapp</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <link href='https://fonts.googleapis.com/css?family=Ubuntu' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=AIzaSyAtMTyEHjwug0bqEL-IEuEKgW3paGNZ0Jk" %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
<body>
    <header class="navbar navbar-inverse" role="navigation">
      <div class="navbar">
        <%= link_to "DoorKnob", '/listings', id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Sign Up",   '/users/new' %></li>
            <li><%= link_to "Login",   login_path %></li>
            <li><%= link_to "Log Out", logout_path %></li>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container">
    	<% if current_user %>
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>" role="alert">
          <% key %><%= value %>
          </div>
    <% end %>
		<div> Hello <%= current_user.username %>, You are logged in as <%= current_user.email %> </div>
    <h3>Where do you want to live? </h3>
    <%= form_tag listings_path, :method => 'get' do %>
      <p>
        <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "Take Me There" %>
      </p>
    <% end %>
		<div class ="pager"><%= link_to 'See All Listings', listings_path, :class => "btn btn-default" %></div>
    <div class="pager"><%= link_to 'Add A New Listing', new_listing_path, :class => "btn btn-default" %></div>
		<% else %>
		<%#= link_to 'login', login_path %>
		<% end %>

    <%= yield %>

		
		<% session[:user_id] %>
		

</body>

</html>
